apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "feoConf.fullname" . }}-test-connection"
  labels:
    {{- include "feoConf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "feoConf.fullname" . }}:{{ .Values.frontend.service.port }}']
  restartPolicy: Never
